let code_arr,greetings;_255‍.x([["default",()=>startRace]]);_255‍.w("./arrays",[["code_arr",["code_arr"],function(v){code_arr=v}],["greetings",["greetings"],function(v){greetings=v}]]);

               function startRace(message) {
	let chosenText = code_arr[Math.floor(Math.random() * code_arr.length)];
	message.channel.send(
		greetings[Math.floor(Math.random() * greetings.length)],
		{
			files: [chosenText.file],
		}
	);
	return chosenText.text;
}
